{
  "name": "botty",
  "version": "0.0.1",
  "description": "Discord Bot",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "set NODE_ENV=development&& pm2 start ecosystem.config.js --watch && pm2 logs",
    "start": "export NODE_ENV=production && pm2 start ecosystem.config.js",
    "restart": "pm2 restart ecosystem.config.js && pm2 logs",
    "stop": "pm2 stop all",
    "lint": "eslint ."
  },
  "author": "Robin Kuiper",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "discord.js": "^12.5.1",
    "eslint": "^7.21.0",
    "express": "^4.17.1",
    "express-handlebars": "^5.2.1",
    "fs": "^0.0.1-security",
    "request": "^2.88.2",
    "sequelize": "^6.5.0",
    "sqlite3": "^5.0.2",
    "twitch": "^4.5.0",
    "twitch-webhooks": "^4.5.0",
    "winston": "^3.3.3"
  }
}
